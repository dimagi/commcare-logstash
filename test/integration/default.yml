---
- name: test-kitchen
  hosts: all
  vars:
    sansible_java_version: 8     
    env_monitoring_id: test
    DATADOG_API_KEY: 123
    filebeat_inputs:
      - type: log
        paths:
          - /var/log/messages
          - /var/log/*.log
  roles:
    - role: filebeat
    - role: logstash
