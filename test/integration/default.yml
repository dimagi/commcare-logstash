---
- name: test-kitchen
  hosts: all
  vars:
    filebeat_inputs:
      - type: log
        paths:
          - /var/log/messages
          - /var/log/*.log
  roles:
    - role: filebeat
    - role: logstash
